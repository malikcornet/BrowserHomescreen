import type { WindowRect } from "../Window";

export const MIN_WINDOW_WIDTH = 280;
export const MIN_WINDOW_HEIGHT = 180;
export const WINDOW_CASCADE_OFFSET = 24;

export const DEFAULT_WINDOW_RECT: WindowRect = {
  x: 24,
  y: 24,
  width: 420,
  height: 320,
};
